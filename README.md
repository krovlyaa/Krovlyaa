# Кровля.Эксперт — лендинг

Статический сайт: HTML + CSS + JS, без сборки. Готов к деплою на GitHub Pages.

Все фотографии в WebP (общий вес папки images/ — ~3.7 МБ, LCP < 1.5 с).

---

## 🚀 Быстрый деплой на GitHub Pages (10 минут)

### Шаг 1. Создайте репозиторий
1. Заходите на github.com → **New repository**
2. Название: любое, например `krovlya-expert`
3. Тип: **Public** (для бесплатного GitHub Pages)
4. Не ставьте галочку «Add README» — у нас уже свой

### Шаг 2. Загрузите файлы
Через веб-интерфейс GitHub:
1. На странице пустого репозитория → **uploading an existing file**
2. Перетащите **всё содержимое папки** (не саму папку) — `index.html`, `styles.css`, `script.js`, папку `images`, все остальные файлы включая `.nojekyll`
3. Commit changes

Через терминал (если умеете):
```bash
git init
git add .
git commit -m "Initial release"
git branch -M main
git remote add origin https://github.com/USERNAME/REPO.git
git push -u origin main
```

### Шаг 3. Включите GitHub Pages
1. В репозитории → **Settings** → **Pages**
2. **Source**: Deploy from a branch
3. **Branch**: `main`, папка `/ (root)` → Save
4. Через 1–2 минуты сайт будет доступен по адресу:
   `https://USERNAME.github.io/REPO/`

### Шаг 4. (Опционально) Свой домен
1. Купите домен где угодно (reg.ru, GoDaddy…)
2. В настройках домена добавьте CNAME запись:
   `www` → `USERNAME.github.io`
3. В GitHub → Settings → Pages → Custom domain: `www.your-domain.ru`

---

## ⚠️ ЧЕК-ЛИСТ ПЕРЕД ЗАПУСКОМ РЕКЛАМЫ

Обязательно проверьте и замените в `index.html` и `script.js`:

### 1. Токен Telegram-бота ⚠️ КРИТИЧНО
Токен `8835407443:...` уже был опубликован в переписке. **Пере-выпустите его**:
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/revoke` → выберите вашего бота → получите новый токен
3. В `script.js` найдите `TELEGRAM_BOT_TOKEN` и замените
4. Если Chat ID у вас другой (не `5042071687`) — тоже замените рядом

### 2. Телефон
Сейчас везде стоит `+7 (995) 442-33-47`. Ctrl+F в редакторе, замените на реальный:
- В `index.html`: 7 мест (хедер × 2, финальный CTA, футер, sticky-плашка)
- В `wa.me/79954423347` — уберите `+` и пробелы: например `+7 495 123-45-67` → `wa.me/74951234567`

### 3. Telegram-канал
Сейчас `https://t.me/krovlya_expert` (несуществующий). Замените на реальный или удалите ссылки.

### 4. Email в футере
Сейчас `info@krovlya-expert.ru`. Замените на реальный или удалите строку с `<a href="mailto:...">`.

### 5. Юр. информация в футере
`ООО «Кровля.Эксперт» · ИНН 0000000000` — замените на реальные ОГРН/ИНН.

### 6. Мета-теги (для рекламы важно)
В `<head>` `index.html` замените:
- `<title>` — на ваш реальный заголовок
- `<meta name="description">` — описание для Google/Яндекс
- `<meta property="og:image">` — можно оставить `og-image.webp`, оно готово

### 7. Sitemap и robots.txt
Замените `YOUR-DOMAIN.github.io` в файлах `robots.txt` и `sitemap.xml` на ваш реальный домен.

### 8. Аналитика (обязательно для рекламы!)
Перед `</head>` в `index.html` вставьте:

**Яндекс.Метрика** (обязательна для Яндекс.Директ):
```html
<!-- Yandex.Metrika counter -->
<script>
   (function(m,e,t,r,i,k,a){m[i]=m[i]||function(){(m[i].a=m[i].a||[]).push(arguments)};
   m[i].l=1*new Date();
   for (var j = 0; j < document.scripts.length; j++) {if (document.scripts[j].src === r) { return; }}
   k=e.createElement(t),a=e.getElementsByTagName(t)[0],k.async=1,k.src=r,a.parentNode.insertBefore(k,a)})
   (window, document, "script", "https://mc.yandex.ru/metrika/tag.js", "ym");
   ym(YOUR_COUNTER_ID, "init", {clickmap:true, trackLinks:true, accurateTrackBounce:true, webvisor:true});
</script>
<!-- /Yandex.Metrika counter -->
```

**Google Analytics** (если планируете Google Ads):
```html
<script async src="https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'G-XXXXXXXXXX');
</script>
```

### 9. Цели/конверсии для рекламы
Рекомендуемые цели в Метрике/GA:
- **Отправка формы** — реальная лид-конверсия. В `script.js` в функции `sendLead` после `markSent()` добавьте:
  ```js
  if (typeof ym !== 'undefined') ym(YOUR_COUNTER_ID, 'reachGoal', 'lead');
  if (typeof gtag !== 'undefined') gtag('event', 'lead');
  ```
- **Клик по WhatsApp** — на всех кнопках/ссылках `wa.me` добавьте `onclick`
- **Использование калькулятора** — после первого движения ползунка
- **Прохождение мини-квиза** — после третьего шага (результат)

### 10. Настройка бота Telegram (безопасность)
- Установите бота приватным: `/setprivacy` в @BotFather → Enable
- Установите разрешение писать только вам: не добавляйте бота в чужие группы
- Если увидите спам от бота — перевыпустите токен ещё раз

---

## 📁 Структура

```
├── index.html            — главная
├── styles.css            — все стили (~1600 строк, дизайн-токены + компоненты)
├── script.js             — интерактив (квиз, калькулятор, слайдер, Telegram, ...)
├── 404.html              — страница «не найдено»
├── thanks.html           — редирект после отправки формы
├── privacy.html          — политика обработки ПД
├── favicon.svg
├── og-image.webp         — превью для соцсетей
├── robots.txt
├── sitemap.xml
├── .nojekyll             — говорит GitHub Pages не запускать Jekyll
└── images/               — 26 WebP-фото (3.7 MB общий вес)
```

---

## 🎨 Что где на сайте

**Верх:**
- Топбар «На июль осталось 2 слота»
- Хедер (прозрачный поверх Hero, при скролле — белый непрозрачный)
- Hero с фото каменного дома на закате

**«Путь боли» — 6 блоков:**
1. Боль №1: «Кинут с деньгами» → Без предоплат
2. Боль №2: «Разведут на доп. работы» → Смета фикс + **мини-квиз** ← точка захвата
3. Боль №3: «Не увижу, что делают» → Директор + фотоотчёт + **портреты Дмитрия и Игоря**
4. Боль №4: «Всучат дешёвку» → 4 материала с ценами
5. Боль №5: «Не знаю сколько это стоит» → **Калькулятор** ← точка захвата
6. Боль №6: «Через 2 года потечёт» → Интерактивная схема кровельного пирога

**Ниже:**
- Виды работ (5 услуг)
- Портфолио (4 кейса со слайдером «до/после»)
- Квиз (4 шага, полный расчёт) ← точка захвата
- Процесс работы (таймлайн 5 шагов)
- FAQ
- Финальный CTA с телефоном/WhatsApp/Telegram/формой ← точка захвата
- Футер с иконками мессенджеров

**Точки конверсии** (в порядке доступности):
- Кнопка «Замер» в хедере — на весь сайт
- Форма в Hero — сразу
- Мини-квиз в блоке боли №2 — быстрый расчёт без контакта
- Калькулятор в блоке боли №5 — интерактивный
- Полный квиз — с контактами
- Финальный CTA — телефон + мессенджеры + форма
- Sticky-плашка WhatsApp внизу экрана на мобильных

---

## ✅ Локальный запуск (для проверки)

```bash
# Установите любой из вариантов
npx serve .
# ИЛИ
python3 -m http.server 8000
```
Открывайте `http://localhost:8000` — сайт запустится.

**Не открывайте `index.html` двойным кликом** — часть браузеров блокирует загрузку фото и `fetch` при протоколе `file://`.

---

## 🐛 Известные ограничения

1. **Токен бота публичен** — неизбежно для статических сайтов. Смягчено honeypot-полем и rate-limit'ом (1 заявка/30 сек). При злоупотреблении — перевыпуск токена.
2. **Форма «Ремонт» в мини-квизе** — использует фото `case-2-before.webp` (зимний дом со старой красной крышей). Если нужно уникальное фото, промпт есть в чате с ассистентом.
3. **Портреты Дмитрия и Игоря** — сгенерированные ИИ. При замене реальными фото сохраните пропорцию (примерно 3:2 landscape, лицо в верхней трети).

---

## 📊 Ожидаемые метрики

При правильной настройке рекламы (Директ / Google Ads):
- **CR на форму** (от посетителя до заявки в Telegram): 3–7%
- **Средняя цена лида** в тематике «монтаж кровли МО»: 500–1500 ₽
- **Bounce rate**: 30–45% (Hero + Боль №1 удерживают внимание)
- **LCP** (Largest Contentful Paint): < 1.5 с на 4G

Проверить эти метрики можно через:
- PageSpeed Insights: https://pagespeed.web.dev/
- WebPageTest: https://www.webpagetest.org/
